For create neu Database you need after start "CustomerReg(Florin)" in the Microsoft Visual Studio, 
in the Package Manager Console execute next command "Update-Database", To create on the SQL-Server neu Database "CustomerRegistrationDB".

Then you can Started yau Projekt on the Microsoft Visual Studio and 
in the same Time run the Angular Projekt "CustomerRegApp". 
You can make that from consol in Angular CLI whit command "ng serve --o"
For acces to application you need to wisit the Page "http://localhost:4200"